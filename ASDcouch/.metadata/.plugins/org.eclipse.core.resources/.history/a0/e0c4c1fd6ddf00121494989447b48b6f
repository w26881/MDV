function(doc) {
  if (doc._id.substr(0,5) === '6c331') {
    emit([doc._id, doc._rev], {
    	"status": doc.status,
    	"name": doc.status,
    	"tel": doc.tel,
    	"notes": doc.notes
    });
  } 
};